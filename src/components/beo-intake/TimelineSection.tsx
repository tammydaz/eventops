import { FormSection } from "./FormSection";

export const TimelineSection = () => {
  return (
    <FormSection title="Event Timeline" icon="⏰">
      {/* Timeline fields moved to Event Details section */}
    </FormSection>
  );
};
